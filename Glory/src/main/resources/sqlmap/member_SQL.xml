<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE  mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">

	<insert id="insert"
		parameterType="com.glory.member.Member" useGeneratedKeys="true" keyProperty="mid">
		<![CDATA[
			INSERT INTO glorymember
			(username, password, mname, memail, mtel) VALUES (#{username}, #{password}, #{mname}, #{memail}, #{mtel})
		]]>
	</insert>

	<select id="select_detail" parameterType="String" resultType="com.glory.member.Member">
	<![CDATA[
		SELECT * FROM glorymember WHERE mid = #{mid}
		]]>
	</select>

	<select id="select_list" resultType="com.glory.member.Member">
	<![CDATA[
		SELECT * FROM glorymember ORDER BY mid DESC
		]]>
	</select>

</mapper>